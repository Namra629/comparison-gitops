apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: order-qmgr-dr-route
spec:
  to:
    kind: Service
    name: order-qmgr-dr-svc
  port:
    targetPort: http
  tls:
    termination: edge
