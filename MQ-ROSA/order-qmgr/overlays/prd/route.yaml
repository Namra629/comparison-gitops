apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: order-qmgr-prd-route
spec:
  to:
    kind: Service
    name: order-qmgr-prd-svc
  port:
    targetPort: http
  tls:
    termination: edge
